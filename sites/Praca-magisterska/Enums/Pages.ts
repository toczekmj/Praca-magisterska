export enum Pages {
    "dashboard" = "/dashboard",
    "settings" = "/settings",
    "files" = "/dashboard/files",
    "charts" = "/dashboard/charts",
    "realtime" = "/dashboard/realtime",
    "login" = "/login",
}